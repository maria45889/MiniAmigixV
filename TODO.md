- [x] Add a `home` view that renders `templates/home.html` (or create it if missing).
- [x] Add URL pattern named `home` so `LOGIN_REDIRECT_URL='home'` resolves.
- [ ] Ensure login POST redirects correctly after successful authentication.
- [ ] (After fix) run server and test login flow.


